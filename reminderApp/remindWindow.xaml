<Window x:Class="reminderApp.remindWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="remindWindow" SizeToContent="WidthAndHeight">
    <Grid>
		<DataGrid HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="1,1,1,1" Height="196" x:Name="dgRemindSet" AutoGenerateColumns="False" IsReadOnly="True">
        <DataGrid.Columns>
        <DataGridTextColumn Header="Дата и время" Binding="{Binding Time,StringFormat=f,ConverterCulture=ru-ru}"></DataGridTextColumn>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding Surname}"></DataGridTextColumn>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}"></DataGridTextColumn>
                <DataGridTextColumn Header="Отчество" Binding="{Binding SecondName}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

	</Grid>
</Window>
